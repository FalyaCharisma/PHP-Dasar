<?php
    // Operator Aritmatika
    // +, -, *, /, %
    $a = 5;
    $b = 2;
    echo $a + $b;
    // echo $a - $b;
    // echo $a * $b;
    // echo $a / $b;
    // echo $a % $b;

    // Menggabungkan teks
    // .
    $nama_depan = "Falya";
    $nama_belakang = "Ayza";
    echo $nama_depan . " " . $nama_belakang;

    // Operator Assignment
    // =, +=, -=, *=, /=, %= .=
    $x = 1;
    $x += 5;
    echo $x;

    // Operator Perbandingan
    // <, >, <=, >=, ==, !=
    var_dump(2 == "2");

    // Operator Identitas
    // ===, !==
    var_dump(2 === "2");

    // Operator Logika
    // &&, ||
    var_dump( 2 > 1 && 1 < 2);
?>